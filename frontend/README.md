# Zenith Frontend

Simple Vite + React + Wagmi app wired to Sepolia deployments.

## Scripts
- npm run dev
- npm run build
- npm run preview

## Env
Create `.env` and set:
- VITE_ALCHEMY_API_KEY or VITE_RPC_URL